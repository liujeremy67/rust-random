notes as i go

Only one owner and one mutable reference per value.

References cannot be null, requires Option in case of absence. No null pointer exceptions.

Cannot store infinitely large types, indirection when referring to instance of self (nodes in linkedlist.rs).

